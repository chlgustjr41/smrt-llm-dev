services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    volumes:
      - ./backend/src:/app/src
      - ./backend/tests:/app/tests
      - smrt-db:/app/.smrt
      # Mount workspace rw so the agent can write .smrt/Project.md
      - .:/workspace
      # Docker socket so the backend can build/run sandbox containers
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - SMRT_BIND_HOST=0.0.0.0
      - SMRT_DB_PATH=/app/.smrt/state.db
      - SMRT_PROJECT_ROOT_ALLOWLIST=/workspace
    # env_file order: later files override earlier ones, so .env wins over
    # backend/.config. backend/.config supplies the agent defaults; .env
    # supplies secrets and any per-deployment overrides.
    env_file:
      - backend/.config
      - .env
    command: uvicorn smrt_agent.main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped
    networks:
      - default
      - smrt-internal

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
    ports:
      - "127.0.0.1:5173:5173"
    command: npm run dev -- --host 0.0.0.0
    restart: unless-stopped

volumes:
  smrt-db:

networks:
  default: {}
  smrt-internal:
    external: true
