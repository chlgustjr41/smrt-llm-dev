# ─── Secrets ────────────────────────────────────────────────
.env
.env.local
.env.*.local
*.pem
*.key
*.p12
*.pfx
credentials.json
service-account*.json
secrets.yaml
secrets.yml

# ─── Backend agent defaults (committed as .config.example) ──
backend/.config

# ─── Agent runtime artifacts ────────────────────────────────
.smrt/runs/
.smrt/sandbox/
.smrt/state.db
.smrt/state.db-journal
.smrt/state.db-wal
.smrt/state.db-shm

# ─── Python ─────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
.tox/

# ─── Node / Frontend ────────────────────────────────────────
node_modules/
.pnp.*
.yarn/cache
.yarn/install-state.gz
.parcel-cache/
.next/
out/
.nuxt/
.cache/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache

# ─── Build outputs ──────────────────────────────────────────
frontend/dist/
frontend/build/
backend/dist/

# ─── IDE / OS ───────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# ─── Claude Code session-local settings (per-developer) ─────
.claude/settings.local.json
.claude/.credentials*

# ─── Docker ─────────────────────────────────────────────────
.docker/

# ─── Eval fixtures: keep fixture source committed, ignore agent-managed state ─
eval-fixtures/*/.smrt/
# Agent-generated docs/READMEs in fixtures — written by Reviewer during
# evaluation runs, not part of the bug-plated baseline. Each fixture has its
# own inner .git that already excludes these locally; this rule keeps them
# out of the meta-repo too so a fresh clone always sees the clean fixture.
eval-fixtures/*/docs/
eval-fixtures/todo-api/README.md
eval-fixtures/inventory-api/README.md
eval-fixtures/wild/*
!eval-fixtures/wild/README.md

# Note: eval-fixtures/todo-api/BUGS.md IS committed to this meta repo (so evaluators
# see the answer key), but it MUST be added to eval-fixtures/todo-api/.gitignore
# (the target project's own gitignore) so the secret_guard_hook hides it from the agent.
